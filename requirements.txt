# SentinelForge — 20-Agent Space Surveillance Engineering System

# Core dependencies (required)
numpy>=1.24.0
torch>=2.0.0

# Cloud pipeline (optional — install for production deployment)
# confluent-kafka>=2.3.0     # Kafka streaming
# psycopg2-binary>=2.9.0     # PostgreSQL/PostGIS
# avro-python3>=1.10.0        # Schema Registry

# CUDA/C++ build (requires NVIDIA CUDA Toolkit >= 11.8)
# Build: cd output/builds/src && mkdir build && cd build
#        cmake .. -DCMAKE_CUDA_ARCHITECTURES=87
#        make -j$(nproc)

# Optional: enhanced astrometry
# astropy>=5.3               # Coordinate frames, time systems
# onnx>=1.14.0               # Model export
# onnxruntime-gpu>=1.15.0    # GPU inference
