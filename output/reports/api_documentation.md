# SentinelForge Edge Pipeline - API Documentation

Generated: 2026-05-01 19:07:55 UTC

## Table of Contents

- [__pycache__](#__pycache__)
- [anomaly_detect.py](#anomaly_detect)
- [api](#api)
- [autonomous.py](#autonomous)
- [background.cu](#background)
- [batch_od.py](#batch_od)
- [benchmark.py](#benchmark)
- [calibration.cu](#calibration)
- [cloud_ingest.py](#cloud_ingest)
- [CMakeLists.txt](#CMakeLists)
- [conjunction_screener.py](#conjunction_screener)
- [coordinate_frames.py](#coordinate_frames)
- [core](#core)
- [correlator.py](#correlator)
- [cpp](#cpp)
- [cuda](#cuda)
- [data](#data)
- [deploy.py](#deploy)
- [fits_handler.py](#fits_handler)
- [hardware](#hardware)
- [monitor.py](#monitor)
- [multi_sensor_fusion.py](#multi_sensor_fusion)
- [observation_scheduler.py](#observation_scheduler)
- [ops](#ops)
- [orbit_propagator.py](#orbit_propagator)
- [photometry.cpp](#photometry)
- [plate_solver.cpp](#plate_solver)
- [regression_guard.py](#regression_guard)
- [science](#science)
- [sgp4_wrapper.py](#sgp4_wrapper)
- [simulate_frames.py](#simulate_frames)
- [source_extract.py](#source_extract)
- [streak_detect.cu](#streak_detect)
- [test_data](#test_data)
- [train_model.py](#train_model)

---

## anomaly_detect.py

**Language:** PY
**Lines:** 253

  > anomaly_detect.py - SentinelForge Edge Pipeline Flags uncorrelated tracks, detects maneuvers, breakup events, and characterizes objects from light curves. Generated by Agent 8 (Anomaly Hunter)
### `class AnomalyType(Enum)`

### `class ObjectClass(Enum)`

### `class Anomaly`

### `class LightCurvePoint`

### `class AnomalyDetector`

  > Detect anomalous behavior in space object observations.
- `def __init__(self)`
- `def analyze_uncorrelated(self, uncorrelated_indices: list,`
  > Flag detections that don't match any known catalog object.
- `def detect_maneuver(self, norad_id: int, observed_ra: float,`
  > Detect if an object has maneuvered (position deviates from prediction).
- `def detect_breakup(self, detections: list,`
  > Detect potential breakup: cluster of uncorrelated tracks near a known object.
- `def _cluster_detections(self, detections: list, radius_deg: float) -> list`
  > Simple single-linkage clustering by angular distance.
- `def characterize_object(self, light_curve: List[LightCurvePoint]) -> dict`
  > Classify object type from brightness time series (light curve).
- `def _estimate_period(self, times: list, values: list) -> Optional[float]`
  > Simple autocorrelation period estimation.
- `def get_alerts(self, min_severity: str = "MEDIUM") -> List[Anomaly]`
  > Get anomalies above severity threshold.


---

## autonomous.py

**Language:** PY
**Lines:** 322

  > autonomous.py - SentinelForge Edge Pipeline Observatory autonomous control state machine. Handles: dome, weather, scheduling, fault recovery, power monitoring. Generated by Agent 5 (Pipeline Orchestrator)
### `class State(Enum)`

### `class WeatherStatus`

- `def safe_to_observe(self) -> bool`
### `class ObservatoryHardware`

  > Interface to physical hardware (overridden per-site).
- `def open_dome(self)`
- `def close_dome(self)`
- `def park_mount(self)`
- `def start_cooling(self)`
- `def acquire_gps_lock(self)`
  > Lock GPS timing receiver. Required for microsecond frame timestamps.
- `def autofocus(self) -> float`
  > Auto-focus via V-curve FWHM minimization. Moves focuser through range, measures FWHM at each step, fits parabola to find optimal position. Temperature-compensated. Returns: best FWHM in arcsec.
- `def start_autoguider(self)`
  > Lock autoguider onto nearest guide star for mount tracking correction.
- `def stop_autoguider(self)`
### `class ObservatoryController`

  > Autonomous observatory state machine. Runs the full night cycle: calibrate -> open -> observe -> close. Handles weather, faults, and power failures.
- `def __init__(self, site_id: str, hardware: ObservatoryHardware,`
- `def transition(self, new_state: State)`
- `def run(self, observe_fn: Callable, calibrate_fn: Callable)`
  > Main loop - call this to run the full night.
- `def _boot(self)`
- `def _idle(self)`
- `def _calibrate(self, calibrate_fn)`
- `def _focus(self)`
  > Auto-focus using V-curve FWHM minimization on a bright star field.
- `def _open_dome(self)`
- `def _observe(self, observe_fn)`
- `def _weather_hold(self)`
- `def _close(self)`
- `def _dawn_shutdown(self)`
- `def _handle_fault(self)`
- `def _emergency_shutdown(self)`
- `def _generate_report(self)`
- `def get_health_telemetry(self) -> dict`
  > Called every 60s by health beacon.


---

## background.cu

**Language:** CU
**Lines:** 212


> background.cu — SentinelForge Edge Pipeline
> GPU-accelerated sky background estimation and subtraction.
> Uses sigma-clipped median on 64x64 tiles.
> Generated by Agent 1 (Frame Architect)

> Device function: Insertion sort for small arrays (in shared memory).
> Used within each tile to compute median.
- `__device__ void insertion_sort(float* arr, int n)`

> Kernel: Compute sigma-clipped median for each tile.
> Each block handles one 64x64 tile.
> Output: one background value per tile.
- `__global__ void tile_median_kernel()`

> Kernel: Bilinear upsample background map to full frame resolution.
> Smooth interpolation avoids tiling artifacts.
- `__global__ void upsample_background_kernel()`
- `float result = v00 * (1-fx)`

> Kernel: Subtract background from science frame.
- `__global__ void subtract_background_kernel()`

### `struct BackgroundBuffers`

- `bool allocate(int frame_width, int frame_height)`
- `void release()`

> SubtractBackground: Full background estimation and subtraction.
- `bool SubtractBackground()`


---

## batch_od.py

**Language:** PY
**Lines:** 262

  > batch_od.py - SentinelForge Cloud Pipeline Batch Least-Squares Orbit Determination (differential correction).  After initial orbit determination (IOD) provides a rough orbit estimate, batch OD refines it by minimizing observation residuals across an entire arc of observations using iterative least-squares.  Method: 1. Predict observation (RA/Dec) from current orbital elements 2. Compute residual (observed - predicted) 3. Compute Jacobian (partial derivatives of obs w.r.t. elements) 4. Solve normal equations: dx = (J^T W J)^-1 J^T W r 5. Update elements: x_new = x + dx 6. Repeat until convergence (||dx|| < threshold)  References: - Vallado, "Fundamentals of Astrodynamics" Ch. 10 - Tapley, Schutz, Born, "Statistical Orbit Determination"
### `class Observation`

  > A single angular observation (RA/Dec from a ground station).
### `class ODSolution`

  > Result of batch orbit determination.
### `class BatchODSolver`

  > Iterative batch least-squares orbit determination. Refines an initial orbit estimate from multiple observations.
- `def __init__(self, max_iterations: int = 20,`
- `def solve(self, observations: List[Observation],`
  > Solve for best-fit orbital elements from observations.  Args: observations: List of RA/Dec observations with timestamps initial_elements: dict with keys a, e, i, raan, argp, M0, epoch  Returns: ODSolution with refined elements and covariance
- `def _predict_observation(self, elements: np.ndarray,`
  > Predict RA/Dec from orbital elements at time offset dt.
- `def _compute_jacobian(self, x: np.ndarray, dt: float,`
  > Numerical Jacobian via central differences.


---

## benchmark.py

**Language:** PY
**Lines:** 121

  > benchmark.py - SentinelForge Test Infrastructure Performance benchmarking against baseline requirements. Generated by Agent 10 (Benchmark Runner)
- `def run_benchmarks(pipeline_module=None, test_data_dir="test_data")`
  > Run all benchmarks and compare to baselines.
- `def measure_latency(pipeline=None) -> float`
  > Measure average frame processing time in ms.
- `def measure_detection_rate(pipeline=None, mag_limit=16) -> float`
  > Measure fraction of injected objects detected.
- `def measure_false_positive_rate(pipeline=None) -> float`
- `def measure_correlation_accuracy(pipeline=None) -> float`


---

## calibration.cu

**Language:** CU
**Lines:** 212


> calibration.cu — SentinelForge Edge Pipeline
> GPU-accelerated dark subtraction and flat field correction.
> Generated by Agent 1 (Frame Architect)
> Target: 4096x4096 @ 16-bit CCD frames
> GPU: NVIDIA Jetson AGX Orin (CUDA 12.2)

> Kernel: Dark Subtraction
> Removes thermal noise pattern captured with shutter closed.
> frame[i] = max(0, frame[i] - dark[i])
- `__global__ void dark_subtract_kernel()`

> Kernel: Flat Field Division
> Normalizes pixel-to-pixel sensitivity variations.
> frame[i] = frame[i] / flat[i]
> flat is pre-normalized to median=1.0
- `__global__ void flat_divide_kernel()`

> Kernel: Convert uint16 raw to float32
> CCD outputs 16-bit unsigned; pipeline works in float32.
- `__global__ void uint16_to_float_kernel()`

> CalibrateFrame: Full calibration pipeline on GPU.
> Steps:
> 1. Convert uint16 raw → float32
> 2. Subtract master dark frame
> 3. Divide by master flat field
> All buffers must be pre-allocated on GPU.
> Returns false on CUDA error.
- `bool CalibrateFrame()`

> Kernel: Accumulate frames for master dark/flat creation.
> Computes running sum for later averaging.
- `__global__ void accumulate_kernel()`

> Kernel: Divide accumulator by N to get average.
- `__global__ void average_kernel()`

> Kernel: Normalize flat field to median=1.0
- `__global__ void normalize_flat_kernel()`

### `struct CalibrationBuffers`

- `bool allocate(int w, int h)`
- `void release()`


---

## cloud_ingest.py

**Language:** PY
**Lines:** 535

  > cloud_ingest.py - SentinelForge Cloud Pipeline Real-time observation ingestion via Apache Kafka + PostGIS catalog storage.  Architecture: Edge sites → Kafka topic 'observations.raw' → Consumer → PostGIS catalog Conjunction alerts → Kafka topic 'alerts.conjunction' → Alert Router  This replaces the batch SQLite model with a production-grade streaming pipeline matching Slingshot's actual tech stack: - Apache Kafka for real-time sensor data streaming - PostgreSQL + PostGIS for spatial catalog queries at scale - Confluent Schema Registry for detection schema evolution  Dependencies: pip install confluent-kafka psycopg2-binary
### `class ObservationProducer`

  > Publishes detection packages from edge sites to Kafka. Runs at each observatory after frame processing.
- `def __init__(self, site_id: str, config: dict = None)`
- `def connect(self)`
  > Initialize Kafka producer.
- `def publish_detections(self, detections: List[dict],`
  > Publish detection batch to Kafka observations topic. Returns number of messages published.  Messages are keyed by site_id for partition affinity — all observations from one site go to the same partition for ordered processing.
- `def publish_alert(self, alert_type: str, data: dict)`
  > Publish urgent alert (conjunction, breakup, maneuver).
- `def _delivery_callback(self, err, msg)`
### `class ObservationConsumer`

  > Consumes detection packages from all edge sites. Routes to multi-sensor fusion pipeline.
- `def __init__(self, group_id: str = "fusion-pipeline", config: dict = None)`
- `def connect(self)`
- `def register_handler(self, topic: str, handler)`
  > Register a callback for a specific topic.
- `def run(self, max_messages: int = None)`
  > Main consumer loop. Polls Kafka, dispatches to handlers. Runs until max_messages reached or interrupted.
### `class CatalogStore`

  > 
- `def __init__(self, dsn: str = None)`
- `def connect(self)`
- `def initialize_schema(self)`
  > self.dsn = dsn or "postgresql://sentinelforge:sf@localhost:5432/catalog" self._conn = None  try: import psycopg2 self._conn = psycopg2.connect(self.dsn) logger.info("Connected to PostGIS catalog") except ImportError: logger.warning("psycopg2 not installed — using mock connection") self._conn = None  Create tables if they don't exist.
- `def upsert_object(self, norad_id: int, elements: dict,`
  > Insert or update a space object's orbital elements and state.
- `def find_nearby_objects(self, x_km: float, y_km: float, z_km: float,`
  > params = { "norad_id": norad_id, "epoch": elements.get("epoch", 0), "a_km": elements.get("a_km", 0), "e": elements.get("e", 0), "i_deg": elements.get("i_deg", 0), "raan_deg": elements.get("raan_deg", 0), "argp_deg": elements.get("argp_deg", 0), "M_deg": elements.get("M_deg", 0), "n_rev_day": elements.get("n_rev_day", 0), "bstar": elements.get("bstar", 0), "x": state_vector.get("x", 0) if state_vector else 0, "y": state_vector.get("y", 0) if state_vector else 0, "z": state_vector.get("z", 0) if state_vector else 0, }  with self._conn.cursor() as cur: cur.execute(sql, params) self._conn.commit()  radius_km: float = 50.0) -> list:
  > if not self._conn: return []  sql =
- `def cone_search(self, ra_deg: float, dec_deg: float,`
  > with self._conn.cursor() as cur: cur.execute(sql, (x_km, y_km, z_km, x_km, y_km, z_km, radius_km)) return cur.fetchall()  radius_arcsec: float) -> list:
  > if not self._conn: return []  radius_deg = radius_arcsec / 3600.0 sql =
- `def get_stale_objects(self, max_hours: float = 24.0) -> list`
  > with self._conn.cursor() as cur: cur.execute(sql, (ra_deg, dec_deg, radius_deg * 111000)) return cur.fetchall()  Find objects that need re-observation.
### `class MockProducer`

- `def produce(self, **kwargs): pass`
- `def flush(self, **kwargs): return 0`
### `class MockConsumer`

- `def poll(self, **kwargs): return None`
- `def subscribe(self, topics): pass`
- `def commit(self, **kwargs): pass`
- `def close(self): pass`


---

## CMakeLists.txt

**Language:** TXT
**Lines:** 61


---

## conjunction_screener.py

**Language:** PY
**Lines:** 415

  > conjunction_screener.py - SentinelForge Cloud Pipeline Collision probability assessment using full 3x3 RTN covariance.  Implements: - Foster-Estes 2D Pc integral with proper encounter-plane projection - Full 3x3 covariance in RTN (Radial-Transverse-Normal) frame - Encounter plane projection via relative velocity vector - Alfano (2005) series expansion for validation  References: - Foster & Estes, "A Parametric Analysis of Orbital Debris Collision Probability and Maneuver Rate for Space Vehicles," NASA JSC-25898, 1992 - Alfano, "A Numerical Implementation of Spherical Object Collision Probability," AIAA/AAS, 2005 - Vallado, Chapter 9: Conjunction Assessment
### `class CovarianceRTN`

  > Full 3x3 position covariance in RTN frame (km^2). RTN = Radial, Transverse (in-track), Normal (cross-track).  The off-diagonal terms are critical for realistic Pc: - RT correlation captures along-track timing uncertainty - RN correlation captures orbital plane uncertainty - TN is typically small but non-zero for perturbed orbits
- `def to_matrix(self) -> np.ndarray`
  > Full symmetric 3x3 covariance matrix.
- `def from_matrix(cls, M: np.ndarray) -> 'CovarianceRTN'`
- `def trace_km(self) -> float`
  > Total position uncertainty (1-sigma sphere radius).
### `class ConjunctionEvent`

  > A predicted close approach between two space objects.
### `class ConjunctionScreener`

  > Screen for collisions using the Foster-Estes method with full encounter-plane covariance projection.  The key improvement over scalar sigma: 1. Combine 3x3 covariances: C_combined = C_primary + C_secondary 2. Project into the encounter plane (perpendicular to relative velocity) 3. Integrate the 2D Gaussian over the hard-body circle
- `def __init__(self, hard_body_radius_m: float = 10.0)`
- `def screen_pair(self,`
  > Screen a single pair for conjunction using full covariance Pc.  Args: pos1/2: ECI position vectors [x,y,z] in km vel1/2: ECI velocity vectors [vx,vy,vz] in km/s cov1/2: Position covariance in RTN frame
- `def _project_to_encounter_plane(self,`
  > Project 3D covariance into the 2D encounter plane.  The encounter plane is defined perpendicular to the relative velocity vector at TCA. This is where the actual miss occurs.  Returns: (C_2d: 2x2 covariance, miss_2d: 2-vector in encounter plane)
- `def _foster_estes_pc(self,`
  > Foster-Estes collision probability integral.  Pc = integral over hard-body circle of the 2D Gaussian PDF defined by the combined covariance.  Uses the eigendecomposition of C_2d to rotate to principal axes, then numerical integration via the series expansion from Alfano (2005).
- `def screen_catalog(self, objects: list) -> List[ConjunctionEvent]`
  > Screen all object pairs. Uses coarse distance pre-filter. Production would use kd-tree in orbital element space.
- `def get_alerts(self, min_level: str = "YELLOW") -> List[ConjunctionEvent]`
  > Get conjunction alerts above threshold.
- `def format_cdm(self, event: ConjunctionEvent) -> dict`
  > Format as Conjunction Data Message (CDM). CDM is the standard format per CCSDS 508.0-B-1.
- `def summary(self) -> dict`


---

## coordinate_frames.py

**Language:** PY
**Lines:** 429

  > coordinate_frames.py - SentinelForge Astrodynamics Library Reference frame transformations: J2000/GCRS ↔ TEME ↔ ITRF ↔ Topocentric  These transforms are critical for comparing TLE-derived predictions (TEME frame) against ground-based observations (topocentric/ITRF frame).  Implements: - Earth orientation: GMST, ERA - Frame rotations: ECI_J2000 → TEME → ITRF → Topocentric (AltAz) - Atmospheric refraction: Bennett 1982 - Precession/Nutation: IAU 1976/1980 (simplified)  References: - Vallado, "Fundamentals of Astrodynamics and Applications" 4th ed. - IERS Conventions (2010), Chapter 5 - Montenbruck & Gill, "Satellite Orbits"
### `class ObserverLocation`

  > Ground station geodetic coordinates (WGS-84).
### `class TopocentricCoords`

  > Topocentric (observer-centered) coordinates.
- `def unix_to_jd(unix_time: float) -> float`
  > Convert Unix timestamp to Julian Date.
- `def jd_to_mjd(jd: float) -> float`
- `def jd_to_centuries(jd: float) -> float`
  > Julian centuries since J2000.0.
- `def greenwich_mean_sidereal_time(jd: float) -> float`
  > GMST in radians. IAU 1982 formula (Vallado eq. 3-47).
- `def earth_rotation_angle(jd: float) -> float`
  > Earth Rotation Angle (ERA) per IERS 2010. More accurate than GMST for modern applications.
- `def _rot_x(angle: float) -> np.ndarray`
- `def _rot_y(angle: float) -> np.ndarray`
- `def _rot_z(angle: float) -> np.ndarray`
- `def precession_matrix(jd: float) -> np.ndarray`
  > IAU 1976 precession matrix: J2000.0 → mean equator of date. Vallado eq. 3-56.
- `def nutation_angles(jd: float) -> Tuple[float, float, float]`
  > IAU 1980 nutation: Δψ (nutation in longitude), Δε (nutation in obliquity). Returns (dpsi, deps, mean_obliquity) in radians. Simplified — uses only the dominant 4 terms.
- `def j2000_to_teme(r_j2000: np.ndarray, jd: float) -> np.ndarray`
  > Convert J2000/GCRS position to TEME (True Equator Mean Equinox). TEME is the native frame for SGP4/TLE predictions.  J2000 → precession → nutation → TEME
- `def teme_to_j2000(r_teme: np.ndarray, jd: float) -> np.ndarray`
  > Inverse: TEME → J2000.
- `def teme_to_itrf(r_teme: np.ndarray, v_teme: np.ndarray,`
  > TEME → ITRF (Earth-fixed frame) via sidereal rotation. Accounts for Earth rotation in velocity transformation.
- `def itrf_to_teme(r_itrf: np.ndarray, v_itrf: np.ndarray,`
  > ITRF → TEME (inverse of above).
- `def geodetic_to_ecef(lat_deg: float, lon_deg: float,`
  > Convert WGS-84 geodetic coordinates to ECEF (ITRF) position vector.
- `def ecef_to_topocentric(r_sat_ecef: np.ndarray, observer: ObserverLocation`
  > Convert satellite ECEF position to topocentric (Az/El/Range).
- `def atmospheric_refraction(elevation_deg: float,`
  > Bennett 1982 atmospheric refraction formula. Returns correction in degrees to ADD to true elevation → apparent elevation.  Critical for low-elevation observations: - At 45°: ~1 arcmin correction - At 10°: ~5 arcmin correction - At 0°:  ~35 arcmin correction (object appears 35' higher than it is)
- `def correct_refraction_radec(ra_deg: float, dec_deg: float,`
  > Apply atmospheric refraction correction to observed RA/Dec. Converts apparent (refracted) → true (geometric) coordinates.  This correction is essential for plate solving: catalog positions are geometric, but observed positions are refracted. Without this, low-elevation plate solutions have 1-3" systematic errors.
- `def eci_to_rtn(r_eci: np.ndarray, v_eci: np.ndarray`
  > Build rotation matrix from ECI to RTN (Radial-Transverse-Normal) frame. RTN is the standard frame for conjunction analysis covariance.  R (Radial): along position vector (nadir-zenith) T (Transverse): in orbital plane, perpendicular to R, in velocity direction N (Normal): orbit normal (completes right-handed system)
- `def covariance_eci_to_rtn(cov_eci: np.ndarray,`
  > Transform 3x3 ECI covariance matrix to RTN frame. cov_rtn = R @ cov_eci @ R^T


---

## correlator.py

**Language:** PY
**Lines:** 137

  > correlator.py - SentinelForge Edge Pipeline Matches detections against local space object catalog using Mahalanobis distance. Generated by Agent 7 (Correlator)
### `class CatalogObject`

  > Space object from local catalog mirror.
### `class CorrelationResult`

  > Result of matching a detection to catalog.
### `class CatalogCorrelator`

  > Match detections to known space objects.
- `def __init__(self)`
- `def load_catalog(self, catalog_path: str)`
  > Load local catalog mirror (JSON format).
- `def mahalanobis_distance(self, det_ra: float, det_dec: float,`
  > 2D Mahalanobis distance accounting for position uncertainty. Returns dimensionless distance (threshold at 4.0).
- `def correlate(self, detections: list) -> Tuple[List[CorrelationResult],`
  > Match each detection against catalog. Returns: (correlation_results, uncorrelated_indices)
- `def summary(self, results: List[CorrelationResult]) -> dict`
  > Generate correlation summary for upload.


---

## deploy.py

**Language:** PY
**Lines:** 304

  > deploy.py - SentinelForge Fleet Management Pushes edge containers to remote observatory sites. Canary -> Staged -> Full rollout with health checks. Generated by Agent 14 (Fleet Deployer)
### `class FleetDeployer`

  > Manages rolling deployments across the observatory network.
- `def __init__(self)`
- `def deploy_canary(self, image_version: str) -> bool`
  > Deploy to canary site first.
- `def deploy_staged(self, image_version: str) -> bool`
  > Deploy in batches after canary passes.
- `def deploy_full(self, image_version: str) -> bool`
  > Full deployment: canary -> staged -> all.
- `def _deploy_to_site(self, site_id: str, image_version: str) -> bool`
  > Push image to a single site via VPN tunnel.
- `def _check_health(self, site_id: str) -> bool`
  > Verify site is healthy post-deploy.
- `def _rollback_site(self, site_id: str)`
  > Revert site to previous image version.
- `def get_report(self) -> dict`


---

## fits_handler.py

**Language:** PY
**Lines:** 176

  > fits_handler.py - SentinelForge Edge Pipeline FITS file I/O with WCS header extraction and frame metadata.  FITS (Flexible Image Transport System) is the universal astronomy data format. Every telescope CCD/CMOS camera outputs FITS files with headers containing: - WCS astrometric solution - Exposure time, filter, gain, CCD temperature - GPS timestamp of shutter open/close  Dependencies: pip install astropy  (optional - graceful fallback to raw binary)
### `class FrameMetadata`

  > Metadata extracted from FITS header or JSON sidecar.
### `class FITSHandler`

  > Read/write FITS files. Falls back to raw binary + JSON sidecar.
- `def __init__(self, output_dir: str = "data/frames")`
- `def read_frame(self, filepath: str) -> Tuple[np.ndarray, FrameMetadata]`
- `def write_frame(self, data: np.ndarray, meta: FrameMetadata,`
- `def _read_fits(self, path: Path) -> Tuple[np.ndarray, FrameMetadata]`
- `def _write_fits(self, data: np.ndarray, meta: FrameMetadata,`
- `def _read_raw(self, path: Path) -> Tuple[np.ndarray, FrameMetadata]`
- `def _write_raw(self, data: np.ndarray, meta: FrameMetadata,`


---

## monitor.py

**Language:** PY
**Lines:** 89

  > monitor.py - SentinelForge Fleet Monitoring Real-time health dashboard for all observatory sites. Generated by Agent 15 (Site Monitor)
### `class SiteMonitor`

  > Monitors health telemetry from all observatory sites.
- `def __init__(self, sites: list)`
- `def update_telemetry(self, site_id: str, data: dict)`
  > Receive health beacon from a site.
- `def _check_alerts(self, site_id: str, data: dict)`
  > Check for anomalous conditions.
- `def dashboard(self)`
  > Print fleet status dashboard.
- `def get_stale_sites(self, timeout_sec=300) -> list`
  > Find sites that haven't reported in.


---

## multi_sensor_fusion.py

**Language:** PY
**Lines:** 303

  > multi_sensor_fusion.py - SentinelForge Cloud Pipeline Fuses observations from 20+ sites into unified tracks. Converts "20 sites saw something" into "this is one object."
### `class Observation`

  > Single observation from one sensor at one time.
### `class Track`

  > A fused track of observations believed to be the same object.
- `def n_sites(self) -> int`
- `def arc_length_sec(self) -> float`
- `def status(self) -> str`
### `class KalmanFilter2D`

  > Simple 2D Kalman filter for RA/Dec tracking. State: [ra, dec, ra_rate, dec_rate] Production would use Unscented KF or Extended KF in 3D ECI.
- `def __init__(self)`
- `def initialize(self, ra: float, dec: float,`
- `def predict(self, dt: float)`
  > Predict state forward by dt seconds.
- `def update(self, ra_obs: float, dec_obs: float,`
  > Kalman update with new observation. Returns innovation (residual) in arcsec.
- `def state(self) -> dict`
### `class MultiSensorFusion`

  > Fuse detections from multiple observatory sites into unified tracks.  Pipeline: 1. Receive detection packages from all edge sites 2. Associate detections to existing tracks (gating) 3. Initialize new tracks for unassociated detections 4. Update track state via Kalman filter 5. Promote/retire tracks based on confidence
- `def __init__(self)`
- `def ingest(self, detections: List[dict], site_id: str) -> dict`
  > Ingest a batch of detections from one site. Returns: {associated, new_tracks, dropped}
- `def _find_best_track(self, obs: Observation) -> Tuple[Optional[int], float]`
  > Find track with smallest residual within gate.
- `def _associate(self, track_id: int, obs: Observation)`
  > Add observation to existing track and update Kalman filter.
- `def _create_track(self, obs: Observation)`
  > Initialize new track from single observation.
- `def _retire_stale_tracks(self, max_age_sec: float = 3600) -> int`
  > Remove tracks not updated in max_age seconds.
- `def get_high_confidence_tracks(self) -> List[Track]`
  > Return tracks confirmed by multiple sites.
- `def summary(self) -> dict`


---

## observation_scheduler.py

**Language:** PY
**Lines:** 225

  > observation_scheduler.py - SentinelForge Cloud Pipeline Tasking optimization: decides which telescope looks where tonight. Maximizes catalog maintenance while prioritizing high-value targets.
### `class TaskPriority(Enum)`

### `class ObservationTask`

  > A single telescope tasking request.
### `class SiteCapability`

  > What a telescope site can observe tonight.
### `class ObservationScheduler`

  > Assigns observation tasks to telescope sites.  Strategy: 1. Build priority queue of targets 2. For each target, find best available site 3. Assign considering: visibility, priority, slew time, weather 4. Re-plan every 30 minutes for dynamic response
- `def __init__(self)`
- `def add_conjunction_task(self, event) -> int`
  > Emergency task: observe conjunction object.
- `def add_catalog_maintenance(self, stale_objects: list) -> int`
  > Routine: re-observe objects with aging predictions.
- `def add_uct_followup(self, track) -> int`
  > Critical: follow up on uncorrelated track.
- `def register_site(self, site_id: str, lat: float, lon: float,`
  > Register an available telescope site for tasking.
- `def is_visible(self, site: SiteCapability,`
  > Check if target is above minimum elevation from site. Simplified: uses declination vs latitude constraint. Full implementation would use sidereal time and hour angle.
- `def generate_schedule(self) -> List[dict]`
  > Assign tasks to sites using greedy priority scheduling.  For each task (by priority): Find best available site (visible + weather + capacity) Assign task to site Update site availability
- `def summary(self) -> dict`
  > Schedule summary for dashboard.


---

## orbit_propagator.py

**Language:** PY
**Lines:** 359

  > orbit_propagator.py - SentinelForge Cloud Pipeline SGP4 + numerical orbit propagation for space object tracking. Bridges edge detections to orbital elements and future position prediction.
### `class OrbitalElements`

  > Classical Keplerian orbital elements (J2000).
- `def __post_init__(self)`
- `def period_sec(self) -> float`
- `def altitude_km(self) -> float`
- `def apogee_km(self) -> float`
- `def perigee_km(self) -> float`
### `class StateVector`

  > Position and velocity in ECI (Earth-Centered Inertial) frame.
- `def r_mag(self) -> float`
- `def v_mag(self) -> float`
- `def to_ra_dec(self) -> Tuple[float, float]`
  > Convert ECI position to RA/Dec (degrees).
### `class OrbitPropagator`

  > Simplified SGP4-like propagator with J2 perturbation. For interview demonstration - production uses SGP4/SDP4 from Vallado.
- `def kepler_to_state(self, elem: OrbitalElements) -> StateVector`
  > Convert Keplerian elements to ECI state vector.
- `def propagate(self, elem: OrbitalElements, dt_sec: float) -> StateVector`
  > Propagate orbit forward by dt seconds with J2 perturbation. Returns new state vector at epoch + dt.
- `def propagate_window(self, elem: OrbitalElements,`
  > Propagate over a time window, return list of state vectors.
- `def _solve_kepler(self, M: float, e: float, tol: float = 1e-12) -> float`
  > Solve Kepler's equation iteratively (Newton-Raphson).
- `def estimate_orbit_regime(self, observations: list) -> Optional[OrbitalElements]`
  > Estimate orbital regime from angular rate of 3+ observations.  This is a fast heuristic classifier — NOT a full angles-only IOD. It maps observed angular rate to approximate altitude bands: > 0.5 deg/s → LEO (~400 km) > 0.1 deg/s → MEO-low (~1,000 km) > 0.01 deg/s → MEO (~10,000 km) else → GEO (~36,000 km)  Production IOD would use Gauss's method (Vallado ch. 7) solving Lambert's problem from 3 line-of-sight observations.
- `def parse_tle(line1: str, line2: str) -> OrbitalElements`
  > Parse NORAD Two-Line Element set into OrbitalElements.


---

## photometry.cpp

**Language:** CPP
**Lines:** 92


> photometry.cpp - SentinelForge Edge Pipeline
> PSF fitting photometry and magnitude measurement.
> Equation: I(x,y) = A * exp(-((x-x0)^2/(2*sx^2) + (y-y0)^2/(2*sy^2))) + B
> Generated by Agent 3 (Plate Solver)

### `struct PhotometryResult`


### `class Photometer`


> Aperture photometry: sum flux in circular aperture,
> subtract background from annulus.
- `static PhotometryResult measure(const float* frame, int width, int height,)`


---

## plate_solver.cpp

**Language:** CPP
**Lines:** 239


> plate_solver.cpp - SentinelForge Edge Pipeline
> Astrometric plate solving: pixel (x,y) -> sky (RA, Dec).
> Algorithm: Astrometric Plate Solving
> Max residual: 0.5 arcsec
> Generated by Agent 3 (Plate Solver)

### `struct PixelSource`


### `struct CatalogStar`


### `struct WCSSolution`

- `void pixel_to_sky(double px, double py, double& ra, double& dec)`

### `struct PSFFitter`


> Fit 2D Gaussian to pixel data around a peak.
> I(x,y) = A * exp(-((x-x0)^2/(2*sx^2) + (y-y0)^2/(2*sy^2))) + B
> Returns sub-pixel centroid (x0, y0).
- `static PixelSource fit(const float* frame, int width, int height,)`

### `struct Triangle`



---

## regression_guard.py

**Language:** PY
**Lines:** 103

  > regression_guard.py - SentinelForge CI/CD Pipeline Blocks any code merge that degrades detection performance. Generated by Agent 12 (Regression Guard)
- `def load_latest_benchmark()`
  > Load most recent benchmark results.
- `def check_regressions(current_results=None)`
  > Compare current benchmarks against baselines and previous results.
- `def run_full_test_suite()`
  > Run complete test suite before merge.


---

## sgp4_wrapper.py

**Language:** PY
**Lines:** 227

  > sgp4_wrapper.py - SentinelForge Astrodynamics Library Production SGP4/SDP4 propagation via Vallado's validated implementation.  SGP4 is the standard USSPACECOM propagator for TLE-based predictions. It includes atmospheric drag (B* term), deep-space lunar/solar perturbations (SDP4 for periods > 225 min), and tesseral resonance corrections.  Our custom J2 propagator (orbit_propagator.py) handles Kepler + J2 secular perturbations only — sufficient for ~1 day LEO, but diverges without drag. SGP4 is required for operational catalog maintenance.  Dependencies: pip install sgp4  (Brandon Rhodes' validated implementation)  References: - Vallado, Crawford, Hujsak, Kelso, "Revisiting Spacetrack Report #3" - Hoots & Roehrich, "Spacetrack Report No. 3" (1980)
### `class SGP4State`

  > Propagated state vector from SGP4.
### `class SGP4Propagator`

  > Wrapper around python-sgp4 library with fallback to custom J2 propagator.  Usage: prop = SGP4Propagator() state = prop.propagate_tle(tle_line1, tle_line2, target_epoch_unix)  The python-sgp4 library is the gold-standard validated implementation. It handles: - Atmospheric drag via the B* parameter - Deep-space perturbations (SDP4) for periods > 225 min - Lunar and solar gravitational effects - Earth tesseral resonance corrections - Secular and periodic J2-J4 perturbations  Our custom J2 propagator only handles: - Keplerian motion + J2 secular terms (RAAN/argp/n drift) - No drag, no third-body, no resonance
- `def __init__(self)`
- `def using_validated_sgp4(self) -> bool`
- `def propagate_tle(self, tle_line1: str, tle_line2: str,`
  > Propagate a TLE to a target epoch.  Args: tle_line1: First line of TLE (69 chars) tle_line2: Second line of TLE (69 chars) target_epoch_unix: Target time (Unix epoch seconds)  Returns: SGP4State in TEME frame (km, km/s)
- `def _propagate_sgp4(self, tle1: str, tle2: str,`
  > Use python-sgp4 library (Vallado validated).
- `def _propagate_fallback(self, tle1: str, tle2: str,`
  > Fallback: use our custom J2 propagator (orbit_propagator.py).
- `def propagate_batch(self, tle_pairs: List[Tuple[str, str]],`
  > Batch propagate multiple TLEs to same epoch. Production optimization: python-sgp4 supports vectorized propagation.
- `def _batch_sgp4(self, tle_pairs: List[Tuple[str, str]],`
  > Vectorized SGP4 using numpy arrays (10-100x faster for large catalogs).


---

## simulate_frames.py

**Language:** PY
**Lines:** 155

  > simulate_frames.py - SentinelForge Test Infrastructure Generates synthetic telescope frames with injected streaks and stars. Ground truth is saved alongside for detection accuracy measurement. Generated by Agent 9 (Data Simulator)
### `class GroundTruth`

  > Known injected source for validation.
- `def generate_background(width: int, height: int,`
  > Generate realistic sky background with Poisson + Gaussian noise.
- `def inject_star(frame: np.ndarray, x: float, y: float,`
  > Inject a Gaussian PSF star at (x, y). Returns actual SNR.
- `def inject_streak(frame: np.ndarray, x: float, y: float,`
  > Inject a linear streak at given angle. Returns actual SNR.
- `def generate_test_frame(n_stars: int = 200, n_streaks: int = 5,`
  > Generate one synthetic frame with ground truth.


---

## source_extract.py

**Language:** PY
**Lines:** 146

  > source_extract.py - SentinelForge Edge Pipeline Assembles detection tables from C++ pipeline outputs. Applies plate solution (WCS) to convert pixel coords to sky coords. Generated by Agent 5 (Pipeline Orchestrator)
### `class Detection`

  > Single detected source/streak in a telescope frame.
### `class WCSSolution`

  > World Coordinate System plate solution.
- `def pixel_to_sky(self, x: float, y: float) -> tuple`
### `class SourceExtractor`

  > Assembles detection table from pipeline outputs.
- `def __init__(self, sensor_id: str)`
- `def process_frame(self, streak_data: list, point_data: list,`
  > Merge streak detections and point source detections, apply WCS transform, and build detection table.
- `def to_json(self) -> str`
- `def to_upload_package(self) -> dict`
  > Minimal package for cloud upload (~10KB per frame).


---

## streak_detect.cu

**Language:** CU
**Lines:** 282


> streak_detect.cu — SentinelForge Edge Pipeline
> GPU-accelerated satellite/debris streak detection.
> Matched filter bank at 180 angles x 5 lengths + peak finder.
> Generated by Agent 2 (GPU Kernel Engineer)

### `struct Detection`


> Estimate local noise level in 32x32 windows.
> Uses MAD (Median Absolute Deviation) — robust to sources.
- `__global__ void estimate_noise_kernel()`

> Apply matched filter at a single angle and length.
> Each thread handles one pixel — sums along the line direction.
> Output: response map (higher = more streak-like).
- `__global__ void matched_filter_kernel()`

> Find local maxima in response map that exceed SNR threshold.
> Uses 5x5 non-maximum suppression window.
> Atomically appends to global detection list.
- `__global__ void peak_detect_kernel()`

### `struct StreakDetector`

- `bool allocate(int w, int h)`

> DetectStreaks: Full detection pipeline.
> Runs matched filter bank at 180 angles x multiple lengths,
> then extracts peaks above SNR threshold.
- `int detect(const float* d_frame, Detection* h_detections, cudaStream_t stream = 0)`
- `void release()`


---

## train_model.py

**Language:** PY
**Lines:** 126

  > train_model.py - SentinelForge ML Pipeline PyTorch streak detection model: trains on synthetic data, exports ONNX. Generated by Agent 6 (ML Trainer)
### `class StreakDetectionNet(nn.Module)`

  > Lightweight CNN for streak vs noise classification.
- `def __init__(self)`
- `def forward(self, x)`
### `class StreakDataset(Dataset)`

  > Dataset of 64x64 cutouts: noise, stars, streaks.
- `def __init__(self, n_samples=10000)`
- `def __len__(self): return self.n`
- `def __getitem__(self, idx)`
- `def train(epochs=20, batch_size=64, lr=1e-3, output_dir="models")`


---

